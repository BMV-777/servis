import React, { useState } from "react";

const SelectPercentage = ({ children, percentage, onSelect }) => {
  // const [sortBy, setSortBy] = useState("dissatisfied");

  // let sortItem;

  // if (sortBy === "dissatisfied") item = Math.round((bill / item) * 100);

  return <div></div>;
};

export default SelectPercentage;
